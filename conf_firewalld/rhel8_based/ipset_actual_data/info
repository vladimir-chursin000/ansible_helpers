Subfolders (pointing to a specific "inventory_host" and "ipset_template_name" in the path) will be created in this directory (using the "IPSET_files_operation.pl" script).

Directory structure
...ipset_actual_data/some_ipset_template_name/some_inventory_host/...
	permanent/...
    	    some_ipset_name.txt
		# One line - one record according to #ipset_type (conf-file "02_conf_ipsets").
	temporary/...
	    some_ipset_name.txt
		# One line - one record according to #ipset_type (conf-file "02_conf_ipsets"), but the record format is "expire datetime;+record associated with ipset_type".
		# Expire datetime has the format "YYYYMMDDHHMISS".
		# Expire date when adding an element to ipset via "ipset input/add" is calculated as follows - current date + #ipset_create_option_timeout.
########################
Do not make manual changes to files in this directory!!!
