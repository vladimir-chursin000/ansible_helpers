---
- hosts: rhel8_firewall_hosts
  tasks:
    - name: "run fwrules_backup_task_main"
      ansible.builtin.include_tasks: "{{playbook_dir}}/tasks/fwrules_backup_task_main.yml"

######################################################

    - name: "run fwrules_local_backup_for_temp_apply_task"
      ansible.builtin.include_tasks: "{{playbook_dir}}/tasks/fwrules_local_backup_for_temp_apply_task.yml"

######################################################

    - name: "run fwrules_backup_collect_raw_network_data_task"
      ansible.builtin.include_tasks: "{{playbook_dir}}/tasks/fwrules_backup_collect_raw_network_data_task.yml"
