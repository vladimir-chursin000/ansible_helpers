- name: remove_prev_fwrules_files_from-fwrules_backup_now (remote-fs)
  ansible.builtin.shell: rm -rf ~/fwrules_backup_now/ && mkdir ~/fwrules_backup_now

######################################################

- name: remove_prev_fwrules_files_from-fwrules_backup_now (remote-fs)
  ansible.builtin.shell: cp -r /etc/firewalld/* ~/fwrules_backup_now
