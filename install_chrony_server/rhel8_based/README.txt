#1) CHECK and CHANGE chrony_server.conf

#2) UPDATE list (for install chrony-client) of ip addresses = chrony_server_hosts

#3) Make sure that the pub-ssh-key is thrown on the hosts (chrony_server_hosts)

#4) Run install_chrony_server.sh
