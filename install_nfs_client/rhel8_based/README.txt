HELPER not READY yet!

#1) CHECK and CHANGE (if needed): .

#2) UPDATE list (for install nfs-clients) of ip addresses = nfs_client_hosts.

#3) Make sure that the pub-ssh-key is thrown to the hosts (nfs_server_hosts).

#4) Run 'install_nfs_client.sh'.

#5) .