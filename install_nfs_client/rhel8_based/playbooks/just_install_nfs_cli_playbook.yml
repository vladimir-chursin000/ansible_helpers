---
- hosts: rhel8_nfs_client_hosts
  tasks:
    - name: install_nfs_packages
      ansible.builtin.dnf:
        name: "{{packages}}"
        state: installed
      vars:
        packages:
        - nfs-utils
        - nfs4-acl-tools
