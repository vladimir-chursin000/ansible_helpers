---
- hosts: rhel8_nfs_client_hosts
  tasks:
    - name: uninstall_nfs_packages
      ansible.builtin.dnf:
        name: "{{item}}"
        state: removed
      with_items:
        - nfs-utils
        - nfs4-acl-tools
